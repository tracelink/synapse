server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:32770/synapse #${JDBC_URL}
    username: synapse #${JDBC_USERNAME}
    password: synapse #${JDBC_PASSWORD}
  thymeleaf:
    cache: true
  flyway:
    locations: classpath:db/core

  #synapse:
  #  encryption:
  #    type: environment
  #    environment:
  #      currentKeyStorePath: ${CURRENT_KEYSTORE}
  #      currentKeyStorePassword: ${CURRENT_KEYSTORE_PASSWORD}
